"""
Entry point for running emocon as a module.

This allows the package to be executed with:
    python -m emocon [command] [options]
"""

from emocon.cli import main

if __name__ == '__main__':
    main()
